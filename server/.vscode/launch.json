{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "🔥 调试后端 (Todo-Think API)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}" 
    }
  ]
}