{
  "name": "java-server",
  "version": "1.0.0",
  "private": true,
  "description": "Todo-Think Java Backend (Spring Boot 2.7 + JDK 8)",
  "scripts": {
    "dev": "node scripts/dev.cjs",
    "start": "npm run dev",
    "build": "node scripts/run-mvn.cjs package -DskipTests",
    "compile": "node scripts/run-mvn.cjs compile -DskipTests"
  }
}
