#前端打包方式

![alt text](image.png)

1. 执行命令 npm ci 安装依赖

2. 执行命令npm run build-xx xx为对应的环境如build-test为测试环境，build-prod为生产环境

3. 找到dist目录，将该目录下的文件拷贝到服务器上

#前端运行方式

1. 执行命令 npm ci 安装依赖

2. 执行命令 npm run serve-xx 运行项目，xx为对应的环境如serve-test为测试环境，serve-dev、local为本地环境
